{
  "installCommand": "rm -rf node_modules && npm install && chmod -R +x node_modules/.bin",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/((?!landing/).*)", "destination": "/index.html" }]
}
